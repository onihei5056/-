# ①〜④の大項目画面(UI構成について.xlsx)の構成と修正手順

出典: **UI構成について.xlsx**(シート「大項目」「付帯設備表」)。
追加指示のたびに手を入れる前提で、**Excelの内容とアプリのコードが1対1になるよう**に作っています。

---

## 1. どのファイルを触ると何が変わるか

| 触るファイル | 変わるもの |
|---|---|
| `app/src/schema/uiSections.ts` | 大項目(①〜④)の**並び順・名称・画面の種類** |
| `app/src/schema/fittings.ts` | 付帯設備表の**行(中項目・小項目・選択肢)** |
| `app/src/components/FittingsForm.tsx` | 付帯設備表の**見た目・入力部品の挙動** |
| `app/src/pages/UiSectionPage.tsx` | 画面の枠(タイトル・保存・前後移動) |

目次タブ・画面のURL・進捗集計は `uiSections.ts` から自動生成されるため、
**大項目を足す・名前を変えるだけなら `uiSections.ts` の1行を編集するだけ**で完結します。

## 2. 現在の実装状況

| 大項目 | 画面ID | 状態 |
|---|---|---|
| ①役所調査 | `ui-city-office` | 大項目名のみ(内容未実装) |
| ②物件調査 | `ui-property` | 大項目名のみ(内容未実装) |
| ③物件状況 | `ui-condition` | 大項目名のみ(内容未実装) |
| ④付帯設備表 | `ui-fittings` | **入力画面を実装済み** |

①②③の内容が決まったら、`uiSections.ts` の `kind` を `'placeholder'` から実装した画面種別へ
変更し、その大項目用のスキーマファイルを `app/src/schema/` に追加してください。

## 3. 付帯設備表の作り

Excelの **2行目〜48行目(47行)をそのままの並び順**で画面にしています。

| Excel列 | 画面での扱い |
|---|---|
| A 大項目 | 画面タイトル(④付帯設備表) |
| B 中項目 | 開閉式の見出し。**空欄の行は直前の中項目に属する**ものとして補完 |
| C 小項目 | 入力行の見出し |
| D チェック方式 | `チェックボックス` / `自由記載` の指定。**この文字列自体は入力欄にしない** |
| E 項目名 | 「有・無」など。**1つだけ選択**(もう一度押すと選択解除) |
| F 2項目目 | 「・」で区切って選択肢に分解。**既定は複数選択** |
| G 追加自由入力項目 | 見出し＋自由入力欄。**記載がある行だけ表示** |
| H | 「判明している故障・不具合の具体的内容(自由記載)」。**全ての入力行に表示** |

### F列の例外(1つだけ選択にしているもの)

`app/src/schema/fittings.ts` の `FITTINGS_EXCLUSIVE_F` に列挙しています。

- `鏡有・鏡無`(浴室洗面台)
- `モニター：有・無`(インターホン)

これ以外のF列は複数選択です。排他にしたいものが増えたら、この配列に文字列をそのまま追加します。

### 「：」より前は見出しになる

`給湯箇所：キッチン・浴室・洗面所` のように「：」がある場合、
`給湯箇所：` を見出しとして表示し、選択肢は `キッチン` `浴室` `洗面所` の3つになります。

### 自由記載の行(備考)

D列が `自由記載` の行(**30行目と48行目**)は、選択肢を持たない自由記述欄です。
中項目名がどちらも「備考」ですが、**行番号で区別しているため別々に保存されます**。

## 4. 入力内容の保存のしかた

他の画面と同じ `sectionAnswers` テーブルに、画面ID(`ui-fittings`)をキーにして保存します。
項目ごとのキーは**Excelの行番号**を含みます。

```
r2_e    … 2行目のE列の選択       (例: "有")
r2_f    … 2行目のF列の選択       (複数選択は配列 例: ["キッチン","浴室"])
r2_g    … 2行目のG列の自由入力
r2_h    … 2行目のH列(故障・不具合)
r30_text … 30行目(備考)の自由記載
```

このため、**中項目名や小項目名が重複していても入力内容が混ざることはありません**
(例: 「混合水栓」はキッチン設備5行目と浴室設備14行目にありますが、`r5_e` と `r14_e` で別物)。

> **注意**: `fittings.ts` の `row` はそのまま保存キーになります。
> 行を並び替えても `row` の値は変えないでください(既に入力済みのデータが読めなくなります)。
> 行を新しく足す場合は、既存の行番号と重ならない番号を付けてください。

## 5. 未確定事項

- 既存アプリの「役所調査(都市計画法・建築基準法など)」画面と、本Excelの「①役所調査」は
  名称が重複しています。統合するのか別画面として並べるのかは**要確認**です
  (現状は別画面として並べています)。
- 付帯設備表の内容は**現時点ではPDF出力に含めていません**。帳票の様式が決まり次第、
  `app/src/pdf/` に追加します。
